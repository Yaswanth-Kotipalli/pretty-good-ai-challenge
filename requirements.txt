# Pinned to the versions this project was verified against.
# The worker uses the livekit-agents 1.3+ API surface (TurnHandlingOptions,
# inference.TurnDetector, voice.turn.InterruptionOptions); earlier 1.x
# releases do not expose those names and will fail at import.
livekit-agents==1.8.2
livekit-api==1.2.1
livekit-plugins-deepgram==1.8.2
livekit-plugins-openai==1.8.2
livekit-plugins-google==1.8.2
livekit-plugins-silero==1.8.2
flask==3.1.3
twilio==9.11.1
openai==2.54.0
python-dotenv==1.2.3
requests==2.34.2
pytest==9.1.1
